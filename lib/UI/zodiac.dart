import 'package:flutter/material.dart';

class ZodiacPage extends StatelessWidget {
  const ZodiacPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Zodiac');
  }
}